[Unit]
Description=Periodically refresh Supernote markdown companions

[Timer]
# Shortly after login, then every 15 minutes.
OnStartupSec=2min
OnUnitActiveSec=15min
# Catch up if the machine was asleep at the scheduled time.
Persistent=true
AccuracySec=1min

[Install]
WantedBy=timers.target
