-- Packages installed ad-hoc via `dcli install` / `dcli search`.
-- This file is auto-managed by dcli. Categorized packages (languages, media,
-- containers, fonts, drivers) live in their dedicated modules and are
-- intentionally not duplicated here.

return {
    description = "Packages installed via dcli install or dcli search commands",
    packages = {},
}
